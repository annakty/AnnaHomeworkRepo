package KnowledgeBaseOfJava;

public class LabSession0312 {

    /**
     * Create an array and store 5 int-values.
     * Print the average of numbers in array.
     */

 //   public static void main(String[] args) {
 //   int[] nuwArr = {1, 4, 8, 45, 76};

    //int together = nuwArr[0] + nuwArr[1] + nuwArr[2] + nuwArr[3] + nuwArr[4];
      //  int average = together / nuwArr.length;
        // System.out.println(average);

        /**
         * String [] names = {"john", "happy", "peAce", "jOy", "LEarN"};
         * String nameToSearch = "";
         *
         * Print the position of searched named.
         * if name not found, print "XXX is not presents in array"
         */

       /** String[] names = {"john", "happy", "peAce", "jOy", "LEarN"};
        String nameToSearch = "peace";
        int position;
        */
    /**
     * if (names[0].equalsIgnoreCase(nameToSearch));{
     * position = 0;
     * else if (names[1].equalsIgnoreCase(nameToSearch)) {
     *position = 1;
     * } else if (names[2].equalsIgnoreCase(nameToSearch)) {
     * position = 2;
     * } else if (names[3].equalsIgnoreCase(nameToSearch)) {
     * position = 3;
     * } else if (names[4].equalsIgnoreCase(nameToSearch)) {
     * position = 4;
     * } else {
     * System.out.println(nameToSearch + "is not presents in the array");
     * }
     */}
